

<div class="post col-md-12">
  
      <div class="row">
        <%= image_tag(@article.feature, class: "parent-width") %>
      </div>
      
      <h2 class="row"><%= @article.title %></h2>
      <p class="row" style='color:gray'>Views: <%= @article.hit %></p>

      <div class="row"><p><%= @markdown.render(@article.body).html_safe %></p></div>
      
      <div class="row tag-div">
        <h5 style="color:#f29810">Tags:</h5>
        <%= render @article.tags %>
        <%= render 'tags/form' %>
      </div>         

	    <%= link_to 'Back', articles_path, class: "row btn btn-info" %>

<div class="row">
	<h3>Comments:</h3>
	<%= render @article.comments %>

	<%= render "comments/form" %>
</div>

</div>
